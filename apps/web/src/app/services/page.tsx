import OfferingsCatalog from '@/components/offerings/OfferingsCatalog';

/**
 * `/services` — catálogo unificado de OFFERINGS. Comparte componente con
 * `/pricing` y permite alternar entre comprar el software o suscribirse al
 * SaaS mensual hospedado por KopTup.
 */
export default function ServicesPage() {
  return <OfferingsCatalog />;
}
